#include <stdio.h>
#include <stdlib.h>

typedef struct binayTree{
    int data;
    struct binaryTree* left;
    struct binaryTree* right;
} heap;

heap* root = NULL;

memalloc

int main() {
    system("pause");
}